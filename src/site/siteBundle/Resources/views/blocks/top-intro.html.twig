<section>
	<div class="container">
		<div class="row">
			
			{% block mainmenu %}
			{{ render(controller('sitesiteBundle:Default:mainmenu')) }}
			{% endblock mainmenu %}

			{% block diaporama %}
			{{ render(controller('sitesiteBundle:Default:diaporama', {slug: 'intro'})) }}
			{% endblock diaporama %}

		</div>
	</div>
</section>